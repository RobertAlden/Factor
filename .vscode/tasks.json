{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Factor File",
            "type": "shell",
            "command": "if [ $(uname) == 'Linux' ]; then factor ${file}; else factor.com -run ${file}; fi",
            "presentation": {
                "reveal": "always"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}